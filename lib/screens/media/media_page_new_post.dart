import 'package:flutter/material.dart';

class MediaPageNewPost extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    List<Widget> mediaPageNewPost = <Widget>[
      Text('新着投稿'),
    ];

    return ListView(children: mediaPageNewPost);
  }
}
